apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    app: app
spec:
  type: ClusterIP
  # По умолчанию без привязки сессии — запросы распределяются между подами.
  sessionAffinity: None
  selector:
    app: app
  ports:
    - name: http
      port: 8280
      targetPort: http
